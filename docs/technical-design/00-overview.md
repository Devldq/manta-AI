# 技术方案 00 — 项目概述与技术愿景 / Technical Overview & Vision

---

## 1. 技术概述

本文档基于 PRD 00 — 项目概述与产品愿景，提供 Manta 平台的**技术实现概述**。技术方案遵循以下原则：

- **渐进增强**：基于现有代码库，不破坏已有功能
- **模块化设计**：高内聚低耦合，便于独立开发和测试
- **类型安全**：TypeScript 严格模式，编译时错误检查
- **本地优先**：v1 阶段所有数据存储在本地

## 2. 技术栈选型

### 2.1 前端技术栈

| 技术 | 版本 | 用途 | 备注 |
|------|------|------|------|
| **Next.js** | 15 | 全栈框架 | App Router, RSC |
| **React** | 19 | UI 库 | Server Components |
| **TypeScript** | 5.x | 类型系统 | 严格模式 |
| **Tailwind CSS** | 4.x | 样式系统 | 原子化 CSS |
| **Zustand** | 5.x | 状态管理 | 轻量级 |
| **lucide-react** | latest | 图标库 | 树摇优化 |

### 2.2 后端技术栈

| 技术 | 版本 | 用途 | 备注 |
|------|------|------|------|
| **Next.js Route Handlers** | 15 | API 层 | RESTful API |
| **SQLite-vec** | latest | 向量数据库 | 本地优先 |
| **文件系统** | - | 数据存储 | JSON 文件 |
| **OpenAI/Anthropic/Ollama** | - | LLM 服务 | 多模型支持 |

### 2.3 开发工具

| 工具 | 用途 | 配置 |
|------|------|------|
| **pnpm** | 包管理 | 快速、节省磁盘空间 |
| **ESLint** | 代码检查 | TypeScript 规则 |
| **Prettier** | 代码格式化 | 统一风格 |
| **Jest/Vitest** | 单元测试 | 高覆盖率 |
| **Playwright** | E2E 测试 | 跨浏览器 |

## 3. 架构设计原则

### 3.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                     客户端层 (Client Layer)                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  React Server Components (RSC)                       │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │ 页面组件  │ │ 业务组件  │ │ 基础组件  │             │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  客户端状态管理 (Zustand)                             │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │ AppStore │ │ RAGStore │ │ WorkflowStore│           │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └──────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                     服务端层 (Server Layer)                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  API 路由层 (Next.js Route Handlers)                   │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │/api/apps │ │/api/rag  │ │/api/workflow│           │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  业务逻辑层 (Services)                                │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │AppService│ │RAGService│ │WorkflowService│         │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  数据访问层 (Repositories)                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │FileStore │ │SQLiteVec │ │MemoryStore│             │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └──────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                     存储层 (Storage Layer)                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  │ 文件系统  │ │ SQLite-vec│ │ 内存缓存  │ │ 向量索引  │     │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心概念映射

| PRD 概念 | 技术实现 | 说明 |
|----------|----------|------|
| **工作空间 (Workspace)** | WorkspaceService + WorkspaceStore | 顶层运行环境，包含多个会话，配置智能体应用、知识库、工作流 |
| **会话 (Conversation)** | ConversationService + 对话管理 | 独立任务对话，支持@调用智能体应用 |
| **Manta AI** | MantaAIService + Agent Loop | 通用智能体，基础对话能力，是智能体应用的基础 |
| **智能体应用 (Agent App)** | AppConfig + 应用服务 | 以Manta AI为基础，拓展RAG、工作流、定制提示词等能力 |
| **知识库 (Knowledge Base)** | RAG 引擎 + 向量数据库 | 文档处理、检索 |
| **工作流 (Workflow)** | 工作流引擎 + 执行器 | 任务编排、执行 |
| **工具 (Tools)** | 工具注册表 + 执行器 | MCP 工具、内置工具 |
| **评估 (Evaluation)** | 评估引擎 + 指标计算 | RAGAs、Agent 评估 |

## 4. 技术约束

### 4.1 兼容性约束

- **现有功能**：不影响 `/tasks`、`/mcp`、`/settings` 页面的功能
- **渐进增强**：现有 Agent 运行能力保持不变，应用层是上层封装
- **Electron 兼容**：所有功能在 Electron 桌面端同样可用

### 4.2 性能目标

| 指标 | 目标 | 测量方法 |
|------|------|----------|
| **首屏加载** | < 1s | Lighthouse Performance |
| **API 响应** | < 500ms | 95th percentile |
| **内存使用** | < 512MB | 运行时监控 |
| **构建时间** | < 30s | CI/CD 流水线 |

### 4.3 安全要求

- **数据本地**：v1 阶段所有数据存储在本地，不依赖云服务
- **输入验证**：所有用户输入进行验证和清理
- **错误处理**：不泄露敏感信息
- **文件权限**：数据目录权限设置为 700

## 5. 实现阶段

### 5.1 Phase 1: 基础架构 (4周)

- 核心数据层 + API 框架
- 存储系统 + 认证框架
- 基础 UI 组件库

### 5.2 Phase 2: 核心功能 (6周)

- 应用管理 + 搭建器
- 知识库引擎
- 工作流引擎

### 5.3 Phase 3: 高级功能 (4周)

- 对话系统 + 记忆系统
- 评估系统
- 集成测试 + 优化

### 5.4 Phase 4: 打磨发布 (2周)

- 性能优化 + 错误处理
- 文档 + 发布准备

## 6. 开发规范

### 6.1 代码规范

- **TypeScript**：严格模式，禁止 `any` 类型
- **ESLint**：遵循 Next.js 推荐规则
- **Prettier**：统一代码格式
- **Git**：Conventional Commits 规范

### 6.2 测试规范

- **单元测试**：覆盖率 > 80%
- **集成测试**：API 端点测试
- **E2E 测试**：核心流程测试
- **性能测试**：关键路径基准测试

### 6.3 文档规范

- **API 文档**：OpenAPI 3.0 规范
- **组件文档**：Storybook 或类似工具
- **架构文档**：Markdown + Mermaid 图表
- **变更日志**：Conventional Changelog

## 7. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **SQLite-vec 性能** | 中 | 高 | 提前性能测试，准备 ChromaDB 备选 |
| **Embedding API 成本** | 高 | 中 | 支持本地 Ollama，设置缓存 |
| **工作流编辑器复杂度** | 高 | 高 | 分阶段实现，先支持串行/并行 |
| **记忆系统效果** | 中 | 中 | 先实现基础功能，后续迭代优化 |
| **评估系统准确性** | 中 | 中 | 使用成熟 RAGAs 框架，人工校验 |

## 8. 依赖管理

### 8.1 核心依赖

```json
{
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.0.0",
    "tailwindcss": "^4.0.0",
    "zustand": "^5.0.0",
    "lucide-react": "^0.400.0",
    "sqlite-vec": "^0.1.0",
    "openai": "^4.0.0",
    "pdf-parse": "^1.1.0",
    "mammoth": "^1.6.0"
  }
}
```

### 8.2 开发依赖

```json
{
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^15.0.0",
    "prettier": "^3.0.0",
    "jest": "^29.0.0",
    "@testing-library/react": "^14.0.0",
    "playwright": "^1.40.0"
  }
}
```

## 9. 部署方案

### 9.1 开发环境

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 运行测试
pnpm test

# 代码检查
pnpm lint
```

### 9.2 生产环境

```bash
# 构建
pnpm build

# 启动
pnpm start

# 环境变量
NEXT_PUBLIC_API_URL=https://api.example.com
DATABASE_URL=sqlite:./data.db
```

### 9.3 Docker 部署

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install

COPY . .
RUN pnpm build

EXPOSE 3000

CMD ["pnpm", "start"]
```

---

## 变更记录 / Changelog

| 日期 | 版本 | 变更说明 |
|------|------|---------|
| 2026-06-14 | v1.0 | 初始版本，基于 PRD 00 生成技术概述 |

---

> 基于：[PRD 00 — 项目概述与产品愿景](../prd/00-overview.md)
> 下一篇：[技术方案 01 — 系统架构](./01-architecture.md)