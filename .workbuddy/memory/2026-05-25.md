# 2026-05-25

## core/ 目录重组

按职责重组了 `core/` 目录结构，解决现有代码组织混乱的问题：

### 新建 core/context/
- 从 `core/chat/` 移入：`system-prompt.ts`、`agent-soul.ts`
- 新建 `index.ts` 统一导出 `buildSystemPrompt`、`readAgentSoul`

### 新建 core/tools/
- 从 `core/conversation/` 移入：`fs-tools.ts` → `file-tools.ts`、`cc-tools.ts` → `shell-tools.ts`、`tools.ts` → `conversation-tools.ts`
- `conversation-tools.ts` 的相对 import `./store` 改为绝对路径 `@/core/conversation/store`
- 新建 `index.ts` 汇总导出所有工具定义

### 精简 core/chat/
- 只保留 Agent Loop 相关：`agent-loop.ts`、`loop-detector/`、`stream-handler/transformer`、`error-formatter`、`message-parser`

### 精简 core/conversation/
- 只剩持久化层：`store.ts`、`types.ts`

### 更新 import 路径
- `core/chat/stream-handler.ts`：`./agent-soul` → `../context/agent-soul`、`./system-prompt` → `../context/system-prompt`
- `core/tool-registry/mcp-setup.ts`：3 个工具 import 全部更新
- `app/api/tools/test/route.ts`：fs-tools import 更新

### 废弃标记
- `core/llm/factory.ts` 标记 `@deprecated`（项目已迁移到 AI SDK，不再用 LangChain）

### 验证
- tsc --noEmit：无新增编译错误，所有报错均为原有类型问题
